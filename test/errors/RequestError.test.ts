import RequestError from '../../lib/errors/RequestError';

describe('RequestError', () => {
  it.skip('should format the error message', () => {
    //TODO
    new RequestError()
  });
  it.skip('should add the `status` property to the error', () => {
    //TODO
    new RequestError()
  });
  it.skip('should add the `description` property to the error', () => {
    //TODO
    new RequestError()
  });
  it.skip('should add the `requestOptions` property to the error', () => {
    //TODO
    new RequestError();
  });
});
